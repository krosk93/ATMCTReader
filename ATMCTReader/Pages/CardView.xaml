<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pages="clr-namespace:ATMCTReader.Pages"
             xmlns:mi="http://www.aathifmahir.com/dotnet/2022/maui/icons"
             xmlns:models="clr-namespace:ATMCTReader.Models;assembly=ATMCTReader.Models"
             xmlns:loccom="clr-namespace:ATMCTReader.Components"
             xmlns:s="clr-namespace:ATMCTReader.Resources.Strings"
             x:DataType="pages:CardViewModel"
             x:Class="ATMCTReader.Pages.CardView"
             Title="{x:Static s:CardView.CardViewer}">
    <ContentPage.BindingContext>
        <pages:CardViewModel />
    </ContentPage.BindingContext>
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="{x:Static s:CardView.Share}" Command="{Binding ShareCardCommand}" IconImageSource="{mi:MaterialOutlined Icon=Share,IconSize=24,IconColor={AppThemeBinding Light={StaticResource Black}, Dark={StaticResource White}}}" />
    </ContentPage.ToolbarItems>
    <ContentPage.Resources>
        <Style x:Key="GridAdaptive" TargetType="Grid">
            <Setter Property="VisualStateManager.VisualStateGroups">
                <VisualStateGroupList>
                    <VisualStateGroup x:Name="WindowSize">
                        <VisualState x:Name="Small">
                            <VisualState.StateTriggers>
                                <AdaptiveTrigger MinWindowWidth="0" />
                            </VisualState.StateTriggers>
                            <VisualState.Setters>
                                <Setter Property="ColumnDefinitions" Value="*" />
                                <Setter Property="RowDefinitions" Value="Auto,Auto,Auto,Auto" />
                            </VisualState.Setters>
                        </VisualState>
                        <VisualState x:Name="Medium">
                            <VisualState.StateTriggers>
                                <AdaptiveTrigger MinWindowWidth="800" />
                            </VisualState.StateTriggers>
                            <VisualState.Setters>
                                <Setter Property="ColumnDefinitions" Value="*,*" />
                                <Setter Property="RowDefinitions" Value="Auto,Auto" />
                            </VisualState.Setters>
                        </VisualState>
                        <VisualState x:Name="Large">
                            <VisualState.StateTriggers>
                                <AdaptiveTrigger MinWindowWidth="1200" />
                            </VisualState.StateTriggers>
                            <VisualState.Setters>
                                <Setter Property="ColumnDefinitions" Value="*,*,*" />
                                <Setter Property="RowDefinitions" Value="Auto,Auto" />
                            </VisualState.Setters>
                        </VisualState>
                        <VisualState x:Name="ExtraLarge">
                            <VisualState.StateTriggers>
                                <AdaptiveTrigger MinWindowWidth="1600" />
                            </VisualState.StateTriggers>
                            <VisualState.Setters>
                                <Setter Property="ColumnDefinitions" Value="*,*,*,*" />
                                <Setter Property="RowDefinitions" Value="Auto,Auto" />
                            </VisualState.Setters>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateGroupList>
            </Setter>
        </Style>
        <Style x:Key="Card" TargetType="loccom:PhysicalCard">
            <Setter Property="MaximumWidthRequest" Value="600" />
            <Setter Property="VisualStateManager.VisualStateGroups">
                <VisualStateGroupList>
                    <VisualStateGroup x:Name="WindowSize">
                        <VisualState x:Name="Small">
                            <VisualState.StateTriggers>
                                <AdaptiveTrigger MinWindowWidth="0" />
                            </VisualState.StateTriggers>
                            <VisualState.Setters>
                                <Setter Property="Grid.Row" Value="0" />
                                <Setter Property="Grid.Column" Value="0" />
                            </VisualState.Setters>
                        </VisualState>
                        <VisualState x:Name="Medium">
                            <VisualState.StateTriggers>
                                <AdaptiveTrigger MinWindowWidth="800" />
                            </VisualState.StateTriggers>
                            <VisualState.Setters>
                                <Setter Property="Grid.Row" Value="0" />
                                <Setter Property="Grid.Column" Value="0" />
                            </VisualState.Setters>
                        </VisualState>
                        <VisualState x:Name="Large">
                            <VisualState.StateTriggers>
                                <AdaptiveTrigger MinWindowWidth="1200" />
                            </VisualState.StateTriggers>
                            <VisualState.Setters>
                                <Setter Property="Grid.Row" Value="0" />
                                <Setter Property="Grid.Column" Value="0" />
                            </VisualState.Setters>
                        </VisualState>
                        <VisualState x:Name="ExtraLarge">
                            <VisualState.StateTriggers>
                                <AdaptiveTrigger MinWindowWidth="1600" />
                            </VisualState.StateTriggers>
                            <VisualState.Setters>
                                <Setter Property="Grid.Row" Value="0" />
                                <Setter Property="Grid.Column" Value="0" />
                            </VisualState.Setters>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateGroupList>
            </Setter>
        </Style>
        <Style x:Key="CurrentTicket" TargetType="VerticalStackLayout">
            <Setter Property="VisualStateManager.VisualStateGroups">
                <VisualStateGroupList>
                    <VisualStateGroup x:Name="WindowSize">
                        <VisualState x:Name="Small">
                            <VisualState.StateTriggers>
                                <AdaptiveTrigger MinWindowWidth="0" />
                            </VisualState.StateTriggers>
                            <VisualState.Setters>
                                <Setter Property="Grid.Row" Value="1" />
                                <Setter Property="Grid.Column" Value="0" />
                            </VisualState.Setters>
                        </VisualState>
                        <VisualState x:Name="Medium">
                            <VisualState.StateTriggers>
                                <AdaptiveTrigger MinWindowWidth="800" />
                            </VisualState.StateTriggers>
                            <VisualState.Setters>
                                <Setter Property="Grid.Row" Value="0" />
                                <Setter Property="Grid.Column" Value="1" />
                            </VisualState.Setters>
                        </VisualState>
                        <VisualState x:Name="Large">
                            <VisualState.StateTriggers>
                                <AdaptiveTrigger MinWindowWidth="1200" />
                            </VisualState.StateTriggers>
                            <VisualState.Setters>
                                <Setter Property="Grid.Row" Value="0" />
                                <Setter Property="Grid.Column" Value="1" />
                            </VisualState.Setters>
                        </VisualState>
                        <VisualState x:Name="ExtraLarge">
                            <VisualState.StateTriggers>
                                <AdaptiveTrigger MinWindowWidth="1600" />
                            </VisualState.StateTriggers>
                            <VisualState.Setters>
                                <Setter Property="Grid.Row" Value="0" />
                                <Setter Property="Grid.Column" Value="1" />
                            </VisualState.Setters>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateGroupList>
            </Setter>
        </Style>
        <Style x:Key="TopUps" TargetType="VerticalStackLayout">
            <Setter Property="VisualStateManager.VisualStateGroups">
                <VisualStateGroupList>
                    <VisualStateGroup x:Name="WindowSize">
                        <VisualState x:Name="Small">
                            <VisualState.StateTriggers>
                                <AdaptiveTrigger MinWindowWidth="0" />
                            </VisualState.StateTriggers>
                            <VisualState.Setters>
                                <Setter Property="Grid.Row" Value="2" />
                                <Setter Property="Grid.Column" Value="0" />
                            </VisualState.Setters>
                        </VisualState>
                        <VisualState x:Name="Medium">
                            <VisualState.StateTriggers>
                                <AdaptiveTrigger MinWindowWidth="800" />
                            </VisualState.StateTriggers>
                            <VisualState.Setters>
                                <Setter Property="Grid.Row" Value="1" />
                                <Setter Property="Grid.Column" Value="0" />
                            </VisualState.Setters>
                        </VisualState>
                        <VisualState x:Name="Large">
                            <VisualState.StateTriggers>
                                <AdaptiveTrigger MinWindowWidth="1200" />
                            </VisualState.StateTriggers>
                            <VisualState.Setters>
                                <Setter Property="Grid.Row" Value="0" />
                                <Setter Property="Grid.Column" Value="2" />
                            </VisualState.Setters>
                        </VisualState>
                        <VisualState x:Name="ExtraLarge">
                            <VisualState.StateTriggers>
                                <AdaptiveTrigger MinWindowWidth="1600" />
                            </VisualState.StateTriggers>
                            <VisualState.Setters>
                                <Setter Property="Grid.Row" Value="0" />
                                <Setter Property="Grid.Column" Value="2" />
                            </VisualState.Setters>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateGroupList>
            </Setter>
        </Style>
        <Style x:Key="LastValidations" TargetType="VerticalStackLayout">
            <Setter Property="VisualStateManager.VisualStateGroups">
                <VisualStateGroupList>
                    <VisualStateGroup x:Name="WindowSize">
                        <VisualState x:Name="Small">
                            <VisualState.StateTriggers>
                                <AdaptiveTrigger MinWindowWidth="0" />
                            </VisualState.StateTriggers>
                            <VisualState.Setters>
                                <Setter Property="Grid.Row" Value="3" />
                                <Setter Property="Grid.Column" Value="0" />
                            </VisualState.Setters>
                        </VisualState>
                        <VisualState x:Name="Medium">
                            <VisualState.StateTriggers>
                                <AdaptiveTrigger MinWindowWidth="800" />
                            </VisualState.StateTriggers>
                            <VisualState.Setters>
                                <Setter Property="Grid.Row" Value="1" />
                                <Setter Property="Grid.Column" Value="1" />
                            </VisualState.Setters>
                        </VisualState>
                        <VisualState x:Name="Large">
                            <VisualState.StateTriggers>
                                <AdaptiveTrigger MinWindowWidth="1200" />
                            </VisualState.StateTriggers>
                            <VisualState.Setters>
                                <Setter Property="Grid.Row" Value="1" />
                                <Setter Property="Grid.Column" Value="0" />
                            </VisualState.Setters>
                        </VisualState>
                        <VisualState x:Name="ExtraLarge">
                            <VisualState.StateTriggers>
                                <AdaptiveTrigger MinWindowWidth="1600" />
                            </VisualState.StateTriggers>
                            <VisualState.Setters>
                                <Setter Property="Grid.Row" Value="0" />
                                <Setter Property="Grid.Column" Value="3" />
                            </VisualState.Setters>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateGroupList>
            </Setter>
        </Style>
    </ContentPage.Resources>
    <ScrollView>
        <Grid Style="{StaticResource GridAdaptive}">
            <loccom:PhysicalCard
                Style="{StaticResource Card}"
                Margin="15"
                AuthorityColor="{Binding AuthorityColor}"
                ExpireDate="{Binding ExpireDate}"
                AccountId="{Binding AccountId}"
                CardId="{Binding CardId}"
                OwnerName="{Binding OwnerName}"
                OwnerSurname1="{Binding OwnerSurname1}"
                OwnerSurname2="{Binding OwnerSurname2}"
                Profile="{Binding Profile}"
            />
            <VerticalStackLayout
                Style="{StaticResource CurrentTicket}"
                Margin="15">
                <Label Text="{x:Static s:CardView.CurrentTicket}" FontSize="Subtitle" FontAttributes="Bold" />
                <Border>
                    <Border.StrokeShape>
                        <RoundRectangle
                            CornerRadius="10" />
                    </Border.StrokeShape>
                    <Grid HorizontalOptions="Fill">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*" />
                            <ColumnDefinition Width="4*" />
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" WidthRequest="40" HeightRequest="40" HorizontalOptions="Center" VerticalOptions="Center" StrokeThickness="0" BackgroundColor="{Binding AuthorityColor}">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="20" />
                            </Border.StrokeShape>
                            <mi:MauiIcon HorizontalOptions="Center" VerticalOptions="Center" Icon="{mi:MaterialOutlined ConfirmationNumber}" IconColor="{StaticResource White}" />
                        </Border>
                        <!-- <loccom:CircleIcon WidthRequest="40" HeightRequest="40" Grid.Column="0" HorizontalOptions="Center" VerticalOptions="Center" Icon="{mi:MaterialOutlinedIcons ConfirmationNumber}" IconColor="{StaticResource White}" /> -->
                        <VerticalStackLayout Grid.Column="1" Padding="10">
                            <Label FontSize="Header" FontAttributes="Bold" Text="{Binding TicketName}" />
                            <ProgressBar Progress="{Binding TicketExpirationProgress}" IsVisible="{Binding ShowExpiration}" />
                            <Label FontSize="Micro" IsVisible="{Binding ShowExpiration}" Text="{Binding TicketExpiration,StringFormat={x:Static s:CardView.ValidUntil}}" />
                            <Border Margin="0,5" IsVisible="{Binding ShowCurrentTicketSeparator}">
                                <Border.StrokeThickness>0.5</Border.StrokeThickness>
                            </Border>
                            <ProgressBar Progress="{Binding TripsProgress}" IsVisible="{Binding ShowTrips}" />
                            <Label FontSize="Micro" Text="{Binding CurrentTicket.TripsLeft,StringFormat={x:Static s:CardView.RemainingTrips}}" IsVisible="{Binding ShowTrips}" />
                        </VerticalStackLayout>
                    </Grid>
                </Border>
            </VerticalStackLayout>
            <VerticalStackLayout
                Style="{StaticResource TopUps}"
                Margin="15">
                <Label Text="{x:Static s:CardView.TopUps}" FontSize="Subtitle" FontAttributes="Bold" />
                <Border>
                    <Border.StrokeShape>
                        <RoundRectangle
                            CornerRadius="10" />
                    </Border.StrokeShape>
                    <CollectionView ItemsSource="{Binding TopUps}">
                        <CollectionView.ItemTemplate>
                            <DataTemplate x:DataType="models:TopUp">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="1*" />
                                        <ColumnDefinition Width="4*" />
                                    </Grid.ColumnDefinitions>
                                    <Border Grid.Column="0" WidthRequest="30" HeightRequest="30" HorizontalOptions="Center" VerticalOptions="Center" StrokeThickness="0" BackgroundColor="{Binding Source={RelativeSource AncestorType={x:Type pages:CardViewModel}},Path=AuthorityColor,x:DataType=pages:CardViewModel}">
                                        <Border.StrokeShape>
                                            <RoundRectangle CornerRadius="15" />
                                        </Border.StrokeShape>
                                        <mi:MauiIcon HorizontalOptions="Center" VerticalOptions="Center" Icon="{mi:MaterialOutlined AddCard}" IconSize="16" IconColor="{StaticResource White}" />
                                    </Border>
                                    <!-- <loccom:CircleIcon WidthRequest="30" HeightRequest="30" Grid.Column="0" HorizontalOptions="Center" VerticalOptions="Center" Icon="{mi:MaterialOutlinedIcons AddCard}" IconColor="{StaticResource White}" /> -->
                                    <VerticalStackLayout Grid.Column="1" Padding="10">
                                        <Label FontSize="Header" FontAttributes="Bold" Text="{Binding Type.Name}" />
                                        <Label FontSize="Micro" Text="{Binding Date,StringFormat='{0:D}'}" />
                                    </VerticalStackLayout>
                                </Grid>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </Border>
            </VerticalStackLayout>
            <VerticalStackLayout
                Style="{StaticResource LastValidations}"
                Margin="15">
                <Label Text="{x:Static s:CardView.LastValidations}" FontSize="Subtitle" FontAttributes="Bold" />
                <Border>
                    <Border.StrokeShape>
                        <RoundRectangle
                            CornerRadius="10" />
                    </Border.StrokeShape>
                    <CollectionView ItemsSource="{Binding Validations}">
                        <CollectionView.ItemTemplate>
                            <DataTemplate x:DataType="models:Validation">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="1*" />
                                        <ColumnDefinition Width="4*" />
                                    </Grid.ColumnDefinitions>
                                    <Border Grid.Column="0" WidthRequest="30" HeightRequest="30" HorizontalOptions="Center" VerticalOptions="Center" StrokeThickness="0" BackgroundColor="{Binding Source={RelativeSource AncestorType={x:Type pages:CardViewModel}},Path=AuthorityColor,x:DataType=pages:CardViewModel}">
                                        <Border.StrokeShape>
                                            <RoundRectangle CornerRadius="15" />
                                        </Border.StrokeShape>
                                        <mi:MauiIcon HorizontalOptions="Center" VerticalOptions="Center" Icon="{mi:MaterialOutlined Contactless}" IconSize="20" IconColor="{StaticResource White}" />
                                    </Border>
                                    <!-- <loccom:CircleIcon WidthRequest="30" HeightRequest="30" Grid.Column="0" HorizontalOptions="Center" VerticalOptions="Center" Icon="{mi:MaterialOutlinedIcons Contactless}" IconColor="{StaticResource White}" /> -->
                                    <VerticalStackLayout Grid.Column="1" Padding="10">
                                        <Label FontSize="Header" FontAttributes="Bold" Text="{Binding Company.Name}" />
                                        <Label FontSize="Small" Text="{Binding Stop.Name}" />
                                        <Label FontSize="Small" Text="{Binding Line.Name}" />
                                        <Label FontSize="Micro" Text="{Binding Instant,StringFormat='{0:f}'}" />
                                    </VerticalStackLayout>
                                </Grid>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </Border>
            </VerticalStackLayout>
        </Grid>
    </ScrollView>
</ContentPage>